[package]
name = "macro-sim"
version = "0.1.0"
edition = "2024"

[dependencies]
rug = { version = "1.27", default-features = false, features = ["integer", "std"] }

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
panic = "abort"
debug = false
